#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const int MOD = 1e9 + 7;

void solve() {
    ll l, r;
    cin >> l >> r;
    ll n = r - l + 1;  // 计算可选范围内的数个数

    ll ans = n;  
    cout << ans << endl;  
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int T; 
    cin >> T;  // 输入测试用例数量
    while (T--) {
        solve();  
    }
    
    return 0;
}
