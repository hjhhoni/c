#include <iostream>
using namespace std;
typedef long long ll;

void solve(){
    enum Weekday{sum,mon,tue,wed,thu,fri,sat};
    // enum Weekday i;
    int i;
    for(i = sum;i<=sat;i++) cout<<i<<endl;
}

int main() {
    ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
    int t=1;//cin>>t
    while(t--) solve();
    return 0;
}